package acidglow.ingamerecipeeditor.recipe.model;

/** The source/state of a recipe known to the editor overlay. */
public enum RecipeOrigin {
    DEFAULT,
    CUSTOM_OVERRIDE,
    NEW_CUSTOM,
    REMOVED_DEFAULT
}
